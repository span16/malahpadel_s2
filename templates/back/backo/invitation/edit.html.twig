{% extends 'back/base.html.twig' %}

{% block title %}Modifier l'Invitation{% endblock %}

{% block body %}
    <div class="container-fluid">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1>Modifier l'invitation</h1>
            <a href="{{ path('back_invitation_index') }}" class="btn btn-secondary">
                <i class="fas fa-arrow-left"></i> Retour à la liste
            </a>
        </div>

        <div class="card">
            <div class="card-body">
                {{ include('back/invitation/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
            </div>
        </div>

        <div class="mt-3">
            {{ include('back/invitation/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %} 