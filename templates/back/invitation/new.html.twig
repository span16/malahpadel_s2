{% extends 'invitation/base.html.twig' %}

{% block title %}Nouvelle invitation{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header">
            <h1 class="h3 mb-0">Créer une nouvelle invitation</h1>
        </div>
        <div class="card-body">
            {{ include('invitation/_form.html.twig') }}
        </div>
        <div class="card-footer">
            <a href="{{ path('app_invitation_index') }}" class="btn btn-secondary">
                <i class="fas fa-arrow-left"></i> Retour à la liste
            </a>
        </div>
    </div>
{% endblock %} 