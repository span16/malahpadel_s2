{% extends 'annonce_match/base.html.twig' %}

{% block title %}Nouvelle annonce{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header">
            <h1 class="h3 mb-0">Créer une nouvelle annonce</h1>
        </div>
        <div class="card-body">
            {{ include('annonce_match/_form.html.twig', {'button_label': 'Créer'}) }}
        </div>
        <div class="card-footer">
            <a href="{{ path('app_annonce_match_index') }}" class="btn btn-secondary">
                <i class="fas fa-arrow-left"></i> Retour à la liste
            </a>
        </div>
    </div>
{% endblock %} 