<form method="post" action="{{ path('app_emploidutemps_delete', {'id_emplois': emploidutemp.id_emplois}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ emploidutemp.id_emplois) }}">
    <button class="btn">Delete</button>
</form>
