{% extends 'base.html.twig' %}

{% block title %}Equipes{% endblock %}

{% block body %}
    <h1>Equipes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Equipe_id</th>
                <td>{{ equipe.equipeId }}</td>
            </tr>
            <tr>
                <th>Nom_equipe</th>
                <td>{{ equipe.nomEquipe }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_equipes_index') }}">back to list</a>

    <a href="{{ path('app_equipes_edit', {'equipe_id': equipe.equipe_id}) }}">edit</a>

    {{ include('equipes/_delete_form.html.twig') }}
{% endblock %}
