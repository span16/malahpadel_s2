<form method="post" action="{{ path('app_recherche_delete', {'id': recherche.userId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ recherche.userId) }}">
    <button class="btn btn-danger">Delete</button>
</form> 